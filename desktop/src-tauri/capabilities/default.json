{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Popup and dashboard windows may call shell commands over IPC.",
  "windows": ["*"],
  "permissions": ["core:default"]
}
