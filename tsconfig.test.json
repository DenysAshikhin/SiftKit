{
  "extends": "./tsconfig.json",
  "compilerOptions": { "noEmit": true, "lib": ["ES2023", "DOM", "DOM.Iterable"] },
  "include": [
    "src/**/*.ts",
    "tests/**/*.ts"
  ],
  "//exclude": "These dashboard tests statically import dashboard/src modules (React tree / controller hooks coupled to JSX tabs + Bundler-resolution extensionless imports). They cannot resolve under the node NodeNext project and are fully typechecked by dashboard/tsconfig.test.json (Bundler resolution, react from dashboard/node_modules) instead. Excluded here to avoid double-coverage, NOT untyped.",
  "exclude": [
    "tests/dashboard-app-refresh.test.ts",
    "tests/dashboard-api.test.ts",
    "tests/dashboard-toasts.test.ts",
    "tests/dashboard-benchmark-controller.test.ts",
    "tests/dashboard-settings-controller.test.ts",
    "tests/dashboard-runs-controller-e2e.test.ts"
  ]
}
