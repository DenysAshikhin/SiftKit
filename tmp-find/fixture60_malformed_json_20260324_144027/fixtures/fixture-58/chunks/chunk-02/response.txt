{"classification":"summary","raw_review_required":true,"output":"The input contains extensive test code and source snippets for drone combat logic, specifically targeting `entities/drone_flat/drone_flat.gd` as the main file for targeting, attack cadence (via `_core_logic_tick`, `lived_ticks`, `target_tick`), projectile logic (`bullet.gd`, `DEFAULT_STATS['damage']`), and tick-driven behavior. Key systems verified include Shield Duality damage spreading (`tests/.../shield_systems.gd`), tech unlock modifiers for targeting modes (`target_lead_1`, `targetting_*`), and death lifecycle callbacks (`_on_kill_callback`). The data is a test suite verifying deterministic damage stacking, target acquisition bookkeeping, and cooldown gating. Raw review required."}